# BIT-Hardware-Lab-2026

北京理工大学计算机科学与技术专业，2026 年大四小学期硬件类实验小组仓库。

本仓库用于计算机组成原理课程设计、汇编与接口技术课程设计。**本页是小组统一执行计划，按“中期完整系统、结项完善”两个阶段推进。任务由执行人完成并自检，自检通过后直接进入下一项。**

> 中期验收：**9 月 11 日**。最终验收：**9 月 18 日**。全部提交物截止：**9 月 18 日 24:00**。以下为小组计划，任务状态需依据实际运行结果更新，不代表已经完成，也不是老师公布的中期合格线。

## 快速导航

- [项目目标与分工](#overview)
- [中期答辩 PPT 大纲](#midterm-ppt)
- [中期任务 M0–M7](#midterm)
- [结项任务 F1–F4](#final-stage)
- [统一技术约定](#interfaces)
- [时间安排与协作](#schedule)
- [资料入口与参考模块](#references)

<a id="overview"></a>
## 一、项目目标与分工

### 1. 固定方案

| 项目 | 方案 |
| --- | --- |
| CPU | 支持 16 条 RV32I 指令的五级流水线 CPU |
| 控制器 | 硬布线控制，使用控制表辅助 AI 生成代码 |
| 数据相关 | 硬件检测与暂停，中期和结项采用相同方案 |
| 控制相关 | 静态分支预测及预测错误恢复 |
| 外设 | 自制 UART 收发控制器 |
| 应用 | CPU 排序、串口输出结果，接收 `r` 后重新运行 |
| 三项 CPU 附加功能 | 溢出检测、静态分支预测、访存地址检查与故障记录 |
| 工具 | Vivado 2019.2、Verilog、RARS |

地址检查与故障记录归入任务书“其他”类附加功能，教师认可口径需要核实。性能统计是课程基本要求，不另外凑作附加功能。两个阶段不安排 Cache、中断、前递升级、VGA 或复杂交互界面。

### 2. 中期成果

中期前接通一套可以独立运行的系统，运行流程固定如下：

1. 复位 CPU。
2. CPU 将 `4、2、5、1、3` 写入数据 RAM。
3. CPU 执行排序。
4. UART 向电脑发送 `SORT: 1 2 3 4 5\r\n`。
5. 电脑发送字符 `r`。
6. CPU 重新初始化数组、排序并输出。

需要交付流水线 CPU、UART 收发与寄存器接口、系统连接、应用汇编与机器码、三项附加功能及测试、单周期复现结果、初步性能计数，以及两门课中期 PPT 和演示记录。

**完整系统仿真通过是内部必达目标，板上完成上述交互是正式目标。** 若仅完成仿真，汇报明确说明尚未完成上板。两个模块分别运行不等于完成系统集成，单周期与 UART 接通也不能称为流水线成果。

### 3. 结项成果

沿用中期架构，扩大测试、修复问题，完成 UART IP 封装、完整性能实验、板上稳定性测试、报告、最终 PPT、视频及个人任务。中期版本单独保存，后续修改保留可恢复的稳定版本。

### 4. 四人分工

| 小组 | 执行人 | 具体工作 |
| --- | --- | --- |
| A：CPU 子系统 | 刘兆钰 | 恢复单周期、控制表与译码、流水线主体、暂停、CPU 模块连接与自检 |
| A：CPU 子系统 | 左逸龙 | 三项附加功能及自检、性能统计与分析、项目协调、本人答辩内容 |
| B：外设与系统运行环境 | 黄奕晨 | UART RTL、时钟复位、存储器与地址译码、板卡配置、系统顶层、IP 封装及自检 |
| B：外设与系统运行环境 | 冯丽嘉 | UART 测试、模拟 CPU 的总线请求、应用汇编、系统测试和结果整理 |

左逸龙始终负责目标、任务分配、进度协调和范围调整，并承担上述技术任务。刘兆钰按计划完成 CPU 主体，不担任代理组长。

**只设置执行人，每个人检查自己的部分，不设置审核人、验收人或逐项审批。** 组内有问题相互协助，跨组按固定接口对接。

A 组先用简单存储器模型测试 CPU，B 组先用模拟总线请求测试 UART。两组独立推进，集成时替换测试替身。

<a id="midterm-ppt"></a>
## 二、中期答辩 PPT 大纲

四人各自制作本人负责部分，统一采用北理工答辩模板。合并后共 **13 页**，讲述约 **13.5 分钟**，为页面切换和提问保留约 1 分钟；每人均控制在 5 分钟以内。

| 讲述人 | 时间 | 页数 | 内容 |
| --- | ---: | ---: | --- |
| 左逸龙 | 4 分钟 | 4 页 | 项目整体、系统方案、三项附加功能、全组进度 |
| 刘兆钰 | 3 分钟 | 3 页 | 指令集、五级流水线、相关处理与 CPU 测试 |
| 黄奕晨 | 3 分钟 | 3 页 | UART、MMIO 与系统环境、板级后续计划 |
| 冯丽嘉 | 3.5 分钟 | 3 页 | 排序应用、系统闭环测试、结项安排 |

### 左逸龙：项目整体与附加功能（第 1–4 页，约 4 分钟）

1. **封面与项目目标**（约 50 秒）
   - 标题：基于 RISC-V 流水线 CPU 与 UART 的计算机系统设计。
   - 对应计组：16 条 RV32I 子集的五级流水线 CPU；对应汇编与接口：自制 UART 与系统集成。
   - 演示目标：排序 `4, 2, 5, 1, 3`，串口输出 `SORT: 1 2 3 4 5`，接收 `r` 后重新执行。
2. **总体结构与四人分工**（约 60 秒）
   - 图示：程序 ROM → 流水线 CPU → RAM / UART MMIO / 溢出状态寄存器 → 串口。
   - 列出四人的实际交付边界，不重复展开后续个人内容。
3. **三项 CPU 附加功能**（约 90 秒）
   - 有符号溢出检测：覆盖 `add`、`sub`、`addi`，输出粘滞标志。
   - BTFNT 静态分支预测：向后条件分支预测跳转，可关闭预测用于后续对比。
   - 地址检查与故障记录：识别未对齐和未映射访问，记录首个故障并停机。
4. **当前进度与结项路径**（约 80 秒）
   - 当前已完成 CPU、UART、排序应用的自动化系统仿真闭环。
   - 已完成 CPU 回归和系统排序重跑测试。
   - 后续完成板级实现、时序检查、上板串口演示、IP 封装和正式性能对比。
   - 明确当前完成的是仿真闭环，板级运行尚待完成。

### 刘兆钰：CPU 设计与验证（第 5–7 页，约 3 分钟）

5. **指令集与硬布线控制**（约 50 秒）
   - 展示控制表的精简版，说明 16 条指令覆盖运算、访存和控制转移。
   - 说明 `opcode`、`funct3`、`funct7` 的严格译码，以及未支持指令不会默认执行。
6. **五级流水线与相关处理**（约 80 秒）
   - 展示 IF、ID、EX、MEM、WB 和四组流水寄存器。
   - 说明 `valid` 标志、暂停时 PC/IF-ID 保持与 ID/EX 空泡。
   - 说明分支或预测错误时清除年轻指令，抑制错误路径副作用。
7. **CPU 自动化测试**（约 50 秒）
   - 分类展示译码、数据相关、分支、故障、溢出和排序测试。
   - 证据：`19/19 passed` 与 `PIPE_SORT_DATA=1,2,3,4,5`。

### 黄奕晨：UART 与系统环境（第 8–10 页，约 3 分钟）

8. **UART 控制器**（约 70 秒）
   - 115200、8N1、10 MHz 工作时钟。
   - TX 状态机、RX 同步与中心采样。
   - STATUS 中的忙、RX 有效、接收溢出、帧错误、忙时重复写状态。
9. **MMIO 与系统环境**（约 60 秒）
   - 展示 RAM、UART TX、UART STATUS、UART RX、CPU 溢出状态五个地址区域。
   - 说明 `system_env` 完成 ROM、RAM、地址译码和 UART 连接。
10. **板级验证计划**（约 50 秒）
    - 当前 UART RTL 已进入系统集成仿真。
    - 后续核对 EES-338 时钟、复位和 UART 引脚，完成实现、时序检查、上板输出和 ILA 调试。
    - 不将 CPU 独立综合或仿真结果表述为整机已经上板通过。

### 冯丽嘉：应用与系统闭环（第 11–13 页，约 3.5 分钟）

11. **排序应用程序**（约 60 秒）
    - 展示流程：初始化数组、冒泡排序、轮询 TX、输出结果、轮询 RX、收到 `r` 后重启。
    - 说明程序只使用 CPU 已支持的 16 条指令，不使用 `ecall`。
12. **完整系统测试**（约 90 秒）
    - 展示完整串口文本与四轮测试标记：`SORT_UART_SYSTEM_PASS text=SORT_1_2_3_4_5_CRLF rounds=4 commands=3`。
    - 说明测试覆盖初次运行、三次 `r` 重跑、字符串逐字节核对、CPU 故障/溢出和 UART 错误状态检查。
13. **中期结论与结项安排**（约 60 秒）
    - 总结 CPU 执行应用、UART 输出、接收命令重跑的仿真闭环。
    - 重申结项前的板级验证、性能实验、IP 封装和材料整理。
    - 保持此页用于进入提问环节，整体问题由左逸龙补充回答。

<a id="midterm"></a>
## 三、中期任务 M0–M7

开始开发前先看[统一技术约定](#interfaces)。以下任务的“通过标准”均由执行人自行检查，并保存结果。

### M0．建立共同工作起点

**执行人：刘兆钰、黄奕晨，各自负责本组工程。预计各 30 分钟。**

- [ ] 保留不修改的[大三单周期工程](参考资料/单周期%20CPU%20参考/左逸龙-1120231863-实验二/)与[原报告](参考资料/单周期%20CPU%20参考/左逸龙-1120231863-实验二.pdf)，另建工作副本。
- [ ] 分开存放 CPU、UART、系统连接、测试、应用和结果，本组新代码不直接写进往年项目目录。
- [ ] 保存共同接口表和地址定义，两组使用同一版本。
- [ ] 每份结果记录对应代码、程序版本及运行方法。
- [ ] 左逸龙核实中期要求及第三项附加功能口径。

**交付与自检：**两组工程均可打开，接口与地址一致，原始资料未被工作副本覆盖。

### M1．恢复单周期并补齐控制表

**执行人：刘兆钰。预计 1.5–2 小时。依赖 M0。**

1. 调整工作副本中 `$readmemh` 的绝对路径。
2. 用原始机器码和数据运行 `cpu_sort_tb`。
3. 检查输出并保存日志、最终内存波形：
   ```text
   sorted_data=1,2,3,4,5
   SORT_PASS
   ```
4. 为 16 条指令建立控制表，逐条写清编码匹配、是否使用 `rs1/rs2`、立即数类型、ALU 操作、访存行为、写回来源和分支类型。
5. 根据控制表用 AI 生成译码代码，增加 `and、or、andi`。
6. 用短程序逐条测试，检查 `x0`、负数比较和立即数扩展。

**交付：**可复跑基线、控制表、16 条指令测试结果。

**自检通过标准：**原排序通过，16 条指令译码明确，未实现指令不被当作加法。控制表生成的代码还需用手工案例和 RARS 结果检查，不能只用同一张表生成实现和预期。

**卡住处理：**90 分钟仍未复现，先查初始化文件、顶层、复位和机器码；再使用组员已验证的候选，B 组继续独立工作。

### M2．实现流水线主体与暂停

**执行人：刘兆钰。预计 4–6 小时。依赖 M1。**

1. 复用 ALU、寄存器堆、立即数生成器，增加四组流水寄存器。
2. 有效标志、PC、目的寄存器、结果和控制信号逐级传递。
3. 运行无依赖短程序，检查写回顺序。
4. 根据控制表判断 ID 指令真正使用哪些源寄存器。
5. 检查这些源寄存器与 EX、MEM、WB 中有效写寄存器指令的依赖。
6. 有依赖时保持 PC 和 IF/ID，向 ID/EX 插入空泡，旧指令继续完成。
7. 在 EX 比较分支、计算目标地址，按接口预留预测、故障和溢出模块连接。
8. 输出有效完成指令及对应 PC。
9. 依次测试 ALU 依赖、load-use、存储依赖、分支依赖，再运行排序主体。

**交付：**CPU 模块、测试程序和运行记录。

**自检通过标准：**寄存器和内存正确；暂停不重复写入；不依赖人工 NOP 掩盖相关性；完成指令统计包含分支和存储；被冲刷指令没有副作用。

### M3．完成三个附加功能

2026-09-09：`codex/zuo-m3` 已修复 `ori/andi`，并完成三个附加功能的 CPU 内部接入；本机 Icarus 功能回归 19/19 通过。参见 [M3 结果、日志与波形](cpu/pipeline/results/m3/README.md) 和 [实施计划](docs/M3实施计划.md)。Vivado 仿真、综合、板级时序及上板仍待验证，UART 排序闭环继续归入 M6。

**执行人：左逸龙，在 M3 分支完成模块、CPU 内部接入及自检；刘兆钰继续负责后续系统连接。根据现有代码检查，修复基线并完成 M3 预计投入 5–8 小时，具体步骤见上述实施计划。**

三个功能分别成模块，逐项独立测试并接入流水线主体。接入依赖修复后的 M2。

#### M3.1 溢出检测

- [ ] 检测 `add、sub、addi` 的有符号溢出。
- [ ] 结果仍保留低 32 位，不增加异常跳转。
- [ ] 指令有效完成时更新粘滞标志，复位清零。
- [ ] 地址计算、空泡、被冲刷指令不更新标志。

| 自检场景 | 预期 |
| --- | --- |
| `0x7FFFFFFF + 1` | 设置溢出 |
| `0x80000000 - 1` | 设置溢出 |
| `1 + 2` | 不溢出 |
| 错误路径中的溢出运算 | 不设置标志 |

#### M3.2 静态分支预测

固定采用 BTFNT：向后条件分支预测跳转，向前条件分支预测不跳转。`jal` 单独处理，不计入条件分支统计。

1. ID 产生预测下一 PC，只有指令实际进入 EX 时执行预测重定向。
2. 预测信息随指令进入 EX。
3. EX 比较实际下一 PC 与预测下一 PC，不一致时恢复正确 PC 并清除年轻指令。
4. 条件分支有效完成时统计分支次数和预测错误次数。
5. 提供 `PREDICT_EN` 参数，关闭时按顺序取指并进行跳转恢复，供性能对比使用。

**自检：**向后跳转成立、循环结束不跳转、向前分支成立与不成立、分支操作数等待均正确；错误路径存储未执行；开关预测不改变最终结果。

#### M3.3 地址检查与故障记录

检查地址未按 4 字节对齐，以及地址不在 RAM 或约定外设寄存器中的情况。

1. 在 MEM 产生读写副作用前检查地址。
2. 故障访问不发出有效总线请求，错误加载不进入有效写回。
3. 记录首次故障 PC、地址和原因。
4. 清除年轻指令并停止取指，允许更老 WB 指令完成。
5. 复位清除故障并恢复运行。

原因编码为 `1=不对齐，2=未映射`，同时出现时优先记录不对齐。

**自检通过标准：**非法访问不破坏 RAM、不触发 UART，故障记录准确。该功能称为故障检测与停机，不称为完整异常处理系统。

### M4．UART 收发与接口

**执行人：黄奕晨负责 RTL 和板卡，冯丽嘉负责测试程序；各自自检。预计各 3–5 小时。依赖 M0，可与 M1–M3 并行。**

黄奕晨：

- [ ] 实现 TX 状态机、位计数、忙状态。
- [ ] 实现 RX 同步、起始位确认、采样和停止位检查。
- [ ] 实现 TX、STATUS、RX 寄存器及缓冲、错误、复位行为。
- [ ] 核对时钟、引脚、复位和电脑串口连接。
- [ ] 独立上板发送 `UART_OK`。

冯丽嘉：

- [ ] 编写 UART 输出采样器，还原发送字节。
- [ ] 编写输入激励，模拟电脑发送字节。
- [ ] 编写模拟 CPU 的寄存器读写请求。
- [ ] 测试 `0x55、0x00、0xFF` 和字符 `r`。
- [ ] 测试忙时写入、读取接收数据、错误停止位和复位。
- [ ] 检查一次总线写入只发送一次。

**交付与自检：**控制器、testbench、日志和板上记录；不接 CPU 也能验证完整寄存器与收发行为。

### M5．系统环境与应用

**执行人：黄奕晨负责硬件环境，冯丽嘉负责应用。预计各 2–3 小时。可与 M4 部分并行，接口行为沿用共同约定。**

黄奕晨：

1. 建立 ROM、RAM、地址译码和系统顶层。
2. RAM 与 UART 写使能互斥。
3. 将 CPU 溢出标志接到只读状态地址。
4. 用模拟总线请求检查 RAM、UART 和地址选择。
5. 提供可直接连接 A 组 CPU 的顶层。

冯丽嘉：

1. 程序初始化五个数组元素并执行排序。
2. 轮询 TX 状态，逐字节输出结果。
3. 输出完成后轮询 RX，收到 `r` 重新初始化并执行，其他字符忽略。
4. 使用 RARS 检查算法，导出并核对实际 CPU 机器码。
5. 确认硬件程序不包含 `ecall`，不直接使用带模拟器打印功能的演示程序。

本应用只显示当前五个一位正整数，不扩展通用整数格式化。负数和重复值的算法测试通过 RAM 结果核对。

**交付与自检：**系统顶层、应用汇编和机器码；程序指令均受支持，地址一致，每次重跑都恢复原始输入。

### M6．完整系统连接与运行

**执行人：刘兆钰、黄奕晨完成连接；冯丽嘉运行系统测试。预计合计 3–5 人时。依赖 M2、M4、M5，最终版本还需接入 M3。**

严格按顺序：

1. CPU 发送一个字符 `A`。
2. CPU 发送固定字符串。
3. CPU 排序并发送结果。
4. 接收 `r` 后重复执行。
5. 接入三个附加功能后重新运行。
6. 完整仿真通过后上板。

系统测试自动核对完整字符串，不能只观察 TX 是否跳变。

**自检通过标准：**复位后输出一次 `SORT: 1 2 3 4 5\r\n`；逐次发送三次 `r`，分别得到正确输出；无重复字节、丢字节和错误内存写入；正常程序不触发故障；附加功能专用测试分别通过。

### M7．中期保存与答辩

**执行人：四人整理本人材料，左逸龙整合现场版本并组织彩排。预计左逸龙投入约 2 小时。依赖本组中期实际完成结果。**

- [ ] 保存稳定代码、机器码、测试脚本与结果。
- [ ] 录制完整运行过程，保存串口文本。
- [ ] 分清单周期、流水线和往年参考结果。
- [ ] 保存初步周期统计和计数说明。
- [ ] 各人检查本人 PPT 的图、数值和完成表述与实际版本一致。
- [ ] 两门课分别保存中期文件，准备组合播放版本。
- [ ] 完整彩排一次，总讲述约 12 分钟，演示计入时间。

| 讲述人 | 内容 | 时间 |
| --- | --- | --- |
| 左逸龙 | 总体方案、三项附加功能 | 3 分钟 |
| 刘兆钰 | 控制表、流水线、暂停及 CPU 测试 | 3 分钟 |
| 黄奕晨 | UART、板卡、系统连接 | 3 分钟 |
| 冯丽嘉 | 应用、完整演示、测试与性能方法 | 3 分钟 |

<a id="final-stage"></a>
## 四、结项任务 F1–F4

### F1．扩大回归测试

**执行人：各人扩展并自检本人负责部分。**

CPU 测试：

- [ ] 已排序、逆序、重复值、负数数组。
- [ ] 两个源寄存器依赖不同指令、连续写同一寄存器。
- [ ] 存储和分支周围的数据依赖。
- [ ] 预测错误后的副作用抑制。
- [ ] 正常地址边界和非法地址。
- [ ] 故障发生时旧指令完成、年轻指令清除。

UART 与系统测试：

- [ ] 连续收发、不同运行时刻复位。
- [ ] 缓冲未读取时的新字节。
- [ ] 同周期消费旧字节并收到新字节。
- [ ] 发送忙时再次写入。
- [ ] 错误帧后的恢复。
- [ ] 重复命令与反复复位。

**自检通过标准：**结果符合约定。每次修改先运行相关测试，再运行完整排序交互。

### F2．IP 封装

**执行人：黄奕晨。**

1. 使用 Vivado Create and Package IP 封装已有 UART 收发与寄存器模块。
2. 包含参数、源文件和必要说明，清除对原测试工程路径的依赖。
3. 在新工程导入 IP，重新运行已有寄存器与收发测试。
4. 保存 IP 包和使用步骤。

**自检通过标准：**新工程可以独立使用该 IP。封装阶段不重写 UART。流程参见 [Vivado 2019.2 官方 IP 指南](https://docs.amd.com/v/u/2019.2-English/ug1118-vivado-creating-packaging-custom-ip)。

### F3．性能实验

**执行人：左逸龙负责计数与分析，冯丽嘉按固定程序运行并记录，各自检查自己的结果。**

比较原单周期 CPU、关闭静态预测的流水线、开启静态预测的流水线。运行无依赖运算、连续数据依赖、五元素排序三类程序，只使用三个版本共同支持的指令。

每次记录：

```text
工程与程序版本、输入、结果、周期数 C、完成指令数 N、
时钟周期、CPI、CPU 时间、条件分支次数、预测错误次数
```

计数规则：

- 从第一条测试指令开始进入执行过程计数，到指定结束标记指令完成时停止。
- 包含流水填充与排空，不包含复位、串口通信、轮询和结束死循环。
- 指令数包含有效分支和存储，不包含空泡及错误路径指令。
- 先用人工可数的短程序检查计数，再测完整程序。
- 大三原始工程、原始数据和本次复测记录分别保留。

```text
CPI = C / N
CPU 时间 = C × 时钟周期
加速比 = 单周期 CPU 时间 / 流水线 CPU 时间
```

主比较统一使用 10 MHz。结果可能不加速，应真实分析暂停、流水填充和预测的影响。原 testbench 的固定 1200 ns 检查时刻不能当作排序耗时，也不能只调快仿真时钟就宣称硬件更快。

### F4．材料与提交

| 材料部分 | 执行人 |
| --- | --- |
| CPU 架构、控制表、暂停说明 | 刘兆钰 |
| 三项附加功能、性能分析 | 左逸龙 |
| UART、系统顶层、板卡、IP | 黄奕晨 |
| 应用、测试程序、结果表 | 冯丽嘉 |
| 各人负责部分的报告与 PPT | 各人 |
| 文档合并、格式统一、提交清单 | 左逸龙 |
| 视频录制 | 黄奕晨、冯丽嘉 |
| 个人日志和两门考试 | 各人 |

各人自行检查内容与代码相符、数值对应原始记录、汇编与机器码匹配，并从提交包重新运行本人负责的部分。所需视频按要求不超过 5 分钟。

团队提交清单：

- [ ] 计组实验报告，根据实际下板情况选择相应报告类型。
- [ ] CPU 源码、测试汇编和机器码、中期及最终 PPT；下板成功时提供演示视频。
- [ ] 接口控制器报告、源码、中期及最终 PPT、接口下板视频。
- [ ] 本组 UART IP 包、运行步骤、功能与性能原始记录一并整理。
- [ ] 四个人分别完成两门课的考试和真实日志。

左逸龙按清单汇总上传，不增加逐项技术审核。9 月 18 日争取 22:00 前上传并回下载检查，硬截止为 24:00。

<a id="interfaces"></a>
## 五、统一技术约定

### 1. CPU 行为

固定 16 条指令：

```text
add sub xor addi ori lui lw sw beq bne blt bge jal and or andi
```

- `x0` 始终为零；只支持对齐的 32 位 `lw/sw`。
- 不实现 `ecall`，NOP、伪指令和标签不另计条数。
- 不采用 MIPS 分支延迟槽。
- 未支持指令不得默认执行为加法，仿真遇到时应报错。
- 指令与数据存储器分开，四组流水寄存器均有有效标志。
- 控制优先级为：复位、较老指令故障、EX 预测纠正、数据等待、ID 预测重定向、正常执行。

### 2. 跨组接口

| 信号 | 方向，相对 CPU | 含义 |
| --- | --- | --- |
| `clk` | 输入 | 系统时钟 |
| `resetn` | 输入 | 低有效复位 |
| `imem_addr[31:0]` | 输出 | 指令字节地址 |
| `imem_rdata[31:0]` | 输入 | 组合读取的指令 |
| `dmem_valid` | 输出 | 一次有效数据访问 |
| `dmem_write` | 输出 | 1 写、0 读 |
| `dmem_addr[31:0]` | 输出 | 数据字节地址 |
| `dmem_wdata[31:0]` | 输出 | 写入数据 |
| `dmem_rdata[31:0]` | 输入 | 组合读取的数据 |

存储器组合读、上升沿写，一条存储指令只产生一次有效写入。CPU 轮询 UART 状态，不增加总线等待协议。空泡、故障访问、被冲刷指令不得发出有效请求。RAM 与 UART 写使能互斥，先解码完整地址，再取 RAM 数组下标。

CPU 额外输出有效完成指令、完成 PC、写回使能、目的寄存器、写回值、溢出标志、故障有效、故障 PC、故障地址、故障原因、条件分支次数和预测错误次数，供仿真及记录使用。

### 3. 地址与 UART

| 地址 | 功能 |
| --- | --- |
| `0x00000000–0x000003FF` | 1 KiB 数据 RAM |
| `0x40000000` | UART TX，写低 8 位启动发送 |
| `0x40000004` | UART STATUS |
| `0x40000008` | UART RX，读取低 8 位并消费该字节 |
| `0x4000000C` | CPU 溢出状态，只读，bit0 为溢出标志 |

STATUS 的 bit0 为发送忙，bit1 为接收数据有效，bit2 为接收缓冲溢出，bit3 为帧错误，bit4 为发送忙时发生额外写入。错误位写 1 清除，其余位由硬件维护。

- 忙时发送新数据：丢弃新数据，设置 bit4。
- 未消费接收数据时收到新字节：保留旧字节，设置 bit2。
- 同周期消费旧字节并收到新字节：保存新字节。
- RX 无数据时读取返回零；写只读寄存器不改变状态；读取 TX 返回零。
- RX 停止位错误时丢弃该帧并记录错误。
- 复位清空状态，TX 回到空闲高电平。

通信格式为 **115200 波特率、8 位数据、无校验、1 位停止位、无流控**。系统时钟为 10 MHz，UART 每位计数 87 周期，RX 采用两级同步与位中心采样。位计数产生使能，不另造逻辑分频时钟。

Clocking Wizard 输入频率、器件与引脚由黄奕晨按实际精工板资料核对。大三工程当前记录的目标器件不能直接沿用到板上工程。

<a id="schedule"></a>
## 六、时间安排与协作

### 1. 中期排程

| 时间 | A 组 | B 组 |
| --- | --- | --- |
| 周三 9/9 上午 | 刘兆钰完成 M0、M1，开始 M2 | M0，开始 UART 和测试 |
| 周三下午 | 流水线、暂停、基本测试 | UART 收发、系统环境与应用 |
| 周三晚上 | 交付 CPU 候选，争取排序通过 | 交付 UART、顶层与应用候选，尝试首次连接 |
| 周四 9/10 上午 | 修控制与相关性问题 | 完成独立测试，推进连接 |
| 周四 13:30–15:30 | 左逸龙完成 M3，刘兆钰连接并自检 | 配合集成测试与上板 |
| 周四 15:30–17:30 | 完整运行与回归 | 完整运行与回归 |
| 周四晚上 | 保存版本、材料、彩排 | 保存版本、材料、彩排 |
| 周五 9/11 | 中期答辩 | 中期答辩 |

预计刘兆钰投入 12–15 小时，黄奕晨、冯丽嘉各 10–14 小时，左逸龙 5–7 小时。估计建立在环境已可用、AI 辅助生成和测试的前提上。板卡由本组持有，开发排程不绑定课表，正式出勤和答辩按课程要求安排。

### 2. 结项排程

| 时间 | 工作 |
| --- | --- |
| 9/11 答辩后 | 保存中期版本，记录反馈 |
| 9/12–9/14 | 扩展测试、IP 封装、性能实验 |
| 9/15 | 数据整理、报告主体 |
| 9/16 | 板上稳定性测试、视频 |
| 9/17 | 各人自检材料、合并文件、彩排 |
| 9/18 | 答辩、上传、回下载检查 |

中期闭环未完成时，首先继续 M6，并据此重排后续时间。

### 3. AI 使用方法

1. 提供任务编号、输入资料、接口、预期结果和允许修改的范围。
2. 明确测试场景，再生成 RTL 或脚本。
3. 实际运行 Vivado，保存输出。
4. 将首个失败、相关波形和最小程序交给 AI 定位。
5. 修改后运行相关测试和完整回归。
6. 保存代码、测试和日志，能解释本人负责的内容。

AI 不自行更换接口、指令集、时钟与地址；两组只修改自己负责的文件；不同时改实现、输入和预期来消除失败。“AI 表示通过”不能代替运行结果。报告中的数据与完成范围来自本组实际记录。

### 4. 简单进度管理

周三中午和晚上，各人更新一次：

```text
任务编号：
现在已经能运行什么：
自检结果和记录位置：
卡在哪里：
下一次交付时间：
```

任务自检通过后标为完成，直接进入下一项。单个问题卡住 45 分钟，整理最小失败例子，请同组成员协助。跨组接口或整体范围变化由左逸龙协调，普通实现问题由执行人自行处理。

### 5. 进度失守时

| 情况 | 处理 |
| --- | --- |
| CPU 延误 | B 组继续用模拟总线完成外设与环境 |
| UART 板上无输出 | 保留集成仿真，单独检查时钟、引脚、串口与复位 |
| 系统无输出 | 回到单字符 `A` 测试，再恢复字符串与排序 |
| 附加功能破坏正常运行 | 保留关闭该功能的稳定版本，单独修复 |
| 只有单周期＋UART | 明确是环境验证，不称为流水线成果 |
| 只有两个独立模块 | 明确闭环尚未完成，重排集成时间 |

<a id="references"></a>
## 七、资料入口与参考模块

### 1. 本届要求与单周期基线

- [实验说明](参考资料/实验说明.md)
- [课程时间安排](参考资料/时间安排.png)
- [左逸龙单周期 CPU 原报告](参考资料/单周期%20CPU%20参考/左逸龙-1120231863-实验二.pdf)
- [左逸龙单周期 CPU 工程](参考资料/单周期%20CPU%20参考/左逸龙-1120231863-实验二/)
- [实验报告模板](参考资料/小学期实验报告模板2021.docx)
- [日志模板](参考资料/日志模板.xlsx)
- [往年项目完整导读](参考资料/往年项目/README.md)

原单周期工程中，`src/Top.v` 是 CPU，`sim/cpu_sort_tb.v` 是排序测试。`riscv1_rars.asm` 带有模拟器打印用的 `ecall`，不能直接作为硬件程序。当前计划从工作副本复现开始，原报告成功记录不等于本轮已复测。

### 2. 按任务查参考，避免反复浏览全部仓库

| 使用者 | 仓库内资料 | 重点与限制 |
| --- | --- | --- |
| A 组 | [2023 CPU 模块目录](参考资料/往年项目/01_往年完整项目/cangtianhuang__BIT-pipelined-cpu/提交材料/CPU/源代码/BIT_pipelined_cpu.srcs/sources_1/new/) | 看流水寄存器、`hazard_unit.v`、`npc.v` 的组织与连接；不直接移植 MIPS 译码或预测策略 |
| A 组 | [2022 四人组 PPT](参考资料/往年项目/01_往年完整项目/ChiZhang-bit__BIT-MIPS-CPU_Design/团队/1120191600.pptx) | 第 12 页暂停思路及后续相关性波形表达，具体控制逻辑自行测试 |
| B 组 | [2022 UART 源码目录](参考资料/往年项目/01_往年完整项目/InkosiZhong__BIT-Hardware-Experiment/flow-line-cpu/flow-line-cpu.srcs/sources_1/imports/source/) | 看 `uart_top.v`、`tx_top.v`、`rx_top.v` 及对应控制器；原 FIFO IP 不连带移植 |
| 两组 | [系统设计讲义](参考资料/往年学长演讲报告.pdf) | 第 23–32 页接口、第 57–65 页外设映射、第 67–84 页流水线与测试 |
| 左逸龙 | [可编辑流水线图](参考资料/往年项目/03_教程与原理图/zan-pu__diagram/) | 参考布局，按本组暂停、预测、故障处理重画 |

往年项目尚未在本组环境中验证。保留引用来源，检查复位、时钟、路径和接口差异，不把旧结果当作本组成果。Vivado 生成目录中的 `.cache` 不代表 CPU 实现了 Cache。

## 八、查看与下载

获得仓库访问权限后，可直接浏览本页和资料链接，或通过 **Code → Download ZIP** 下载，也可以克隆仓库。往年项目以普通文件保存，无需额外下载子模块，来源及下载时提交号见往年项目导读和下载清单。

本 README 作为当前计划的统一入口，任务进展依据实际自检记录更新。本仓库保持私有，仅供小组校内学习协作。课程资料请勿外传；往年项目保留作者署名与许可证，其内容不代表本组已完成的实验成果。`*.code-workspace` 属于本地配置，不上传。
